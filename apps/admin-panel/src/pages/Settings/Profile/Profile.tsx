const Profile = () => {
  return <div>Profile</div>;
};

// eslint-disable-next-line react-refresh/only-export-components
export const ProfileNavigationRoute = {
  element: <Profile />,
  path: "/settings/profile"
};

export default Profile;
