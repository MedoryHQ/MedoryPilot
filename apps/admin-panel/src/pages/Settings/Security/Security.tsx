const Security = () => {
  return <div>Security</div>;
};

// eslint-disable-next-line react-refresh/only-export-components
export const SecurityNavigationRoute = {
  element: <Security />,
  path: "/settings/security"
};

export default Security;
