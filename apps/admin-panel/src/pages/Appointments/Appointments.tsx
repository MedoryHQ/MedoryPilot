const Appointments = () => {
  return <div>Appointments</div>;
};

// eslint-disable-next-line react-refresh/only-export-components
export const AppointmentsNavigationRoute = {
  element: <Appointments />,
  path: "/appointments"
};

export default Appointments;
