const Home = () => {
  return <div>Home</div>;
};

// eslint-disable-next-line react-refresh/only-export-components
export const HomeNavigationRoute = {
  element: <Home />,
  path: "/landing"
};

export default Home;
