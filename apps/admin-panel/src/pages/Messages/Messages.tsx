const Messages = () => {
  return <div>Messages</div>;
};

// eslint-disable-next-line react-refresh/only-export-components
export const MessagesNavigationRoute = {
  element: <Messages />,
  path: "/messages"
};

export default Messages;
